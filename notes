GCP configuration
=================

Give VM an external IP.
Set up firewall to only allow https from cloudflare IP addresses, don't allow http from anyone.

Clean apt cache regularly - add
APT::Periodic::CleanInterval "7";
to /etc/apt/apt.conf.d/02periodic
